# DeepKidney
A geometric deep learning approach to the kidney exchange matching problem.

## Dataset
Located in /data, each graph would be in its individual file. A csv file would be in each directory, mapping each graph to its solution. 

